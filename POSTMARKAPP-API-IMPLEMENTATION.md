Mimic postmarkapp API server so gomailserver can be used as a direct replacement for postmarkapp.

Provide management interface in the existing Admin UI just.

Utilizes the postmarkapp API and fully compatible with the postmarkapp client API libraries.